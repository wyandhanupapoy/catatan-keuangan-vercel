<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Catatan Keuangan</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; }
        .card-pemasukan { border-left: 5px solid #198754; }
        .card-pengeluaran { border-left: 5px solid #dc3545; }
        .card-saldo { border-left: 5px solid #0d6efd; }
    </style>
</head>
<body>
    <div class="container my-4">
        <header class="text-center mb-4">
            <h1>Catatan Keuanganku 💰</h1>
            <p class="lead">Kelola pemasukan dan pengeluaranmu dengan mudah.</p>
        </header>

        <section class="row g-3 text-center mb-4">
            <div class="col-md-4">
                <div class="card card-pemasukan shadow-sm h-100"><div class="card-body">
                    <h5 class="card-title text-success">Total Pemasukan</h5>
                    <h3>Rp <%= pemasukan.toLocaleString('id-ID') %></h3>
                </div></div>
            </div>
            <div class="col-md-4">
                <div class="card card-pengeluaran shadow-sm h-100"><div class="card-body">
                    <h5 class="card-title text-danger">Total Pengeluaran</h5>
                    <h3>Rp <%= pengeluaran.toLocaleString('id-ID') %></h3>
                </div></div>
            </div>
            <div class="col-md-4">
                <div class="card card-saldo shadow-sm h-100"><div class="card-body">
                    <h5 class="card-title text-primary">Saldo Akhir</h5>
                    <h3>Rp <%= saldo.toLocaleString('id-ID') %></h3>
                </div></div>
            </div>
        </section>

        <section class="card shadow-sm mb-4">
            <div class="card-body">
                <h5 class="card-title mb-3">Tambah Transaksi Baru</h5>
                <form action="/tambah" method="POST" class="row g-3">
                    <div class="col-md-6">
                        <label for="deskripsi" class="form-label">Deskripsi</label>
                        <input type="text" id="deskripsi" name="deskripsi" class="form-control" placeholder="Contoh: Gaji bulanan" required>
                    </div>
                    <div class="col-md-6">
                        <label for="jumlah" class="form-label">Jumlah (Rp)</label>
                        <input type="number" id="jumlah" name="jumlah" class="form-control" placeholder="50000" required>
                    </div>
                    <div class="col-md-6">
                        <label for="tipe" class="form-label">Tipe Transaksi</label>
                        <select id="tipe" name="tipe" class="form-select" required>
                            <option value="pemasukan">Pemasukan</option>
                            <option value="pengeluaran">Pengeluaran</option>
                        </select>
                    </div>
                    <div class="col-md-6">
                        <label for="tanggal" class="form-label">Tanggal</label>
                        <input type="date" id="tanggal" name="tanggal" class="form-control" required>
                    </div>
                    <div class="col-12">
                        <button type="submit" class="btn btn-primary w-100">Simpan Transaksi</button>
                    </div>
                </form>
            </div>
        </section>

        <section class="card shadow-sm">
            <div class="card-body">
                <h5 class="card-title mb-3">Riwayat Transaksi</h5>
                <div class="table-responsive">
                    <table class="table table-hover align-middle">
                        <thead class="table-light">
                            <tr>
                                <th>Tanggal</th>
                                <th>Deskripsi</th>
                                <th class="text-end">Jumlah</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if(transaksi.length === 0) { %>
                                <tr><td colspan="3" class="text-center py-4">Belum ada transaksi.</td></tr>
                            <% } else { %>
                                <% transaksi.forEach(t => { %>
                                    <tr>
                                        <td><%= new Date(t.tanggal).toLocaleDateString('id-ID', {day: '2-digit', month: 'short', year: 'numeric'}) %></td>
                                        <td><%= t.deskripsi %></td>
                                        <td class="text-end fw-bold <%= t.tipe === 'pemasukan' ? 'text-success' : 'text-danger' %>">
                                            <%= (t.tipe === 'pemasukan' ? '+ ' : '- ') %>Rp <%= parseFloat(t.jumlah).toLocaleString('id-ID') %>
                                        </td>
                                    </tr>
                                <% }) %>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </section>

        <footer class="text-center text-muted mt-5">
            <p>Dibuat dengan ❤️ di Cimenyan &copy; 2025</p>
        </footer>
    </div>
</body>
</html>